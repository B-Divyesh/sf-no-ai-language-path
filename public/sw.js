// This placeholder is replaced in dist/sw.js by scripts/generate-sw.mjs.
// Development intentionally has no offline cache: production builds must use
// the generated file so its precache exactly matches Vite's hashed assets.
